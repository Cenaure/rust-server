services:
  rust-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rust-server
    ports:
      - "8080:8080"
    env_file:
      - .env
    restart: unless-stopped
